var playlist = { artistName: "songTitle" }

function updatePlaylist(Object, String, songTitle){
  return object.assign({}, playlist, { Slowdive: ['Alison']}) return object.assign({}, playlist, { My Bloody Valentine: ['Sometimes'] })
}